import { jsx as _jsx } from "react/jsx-runtime";
import { PropertiesBrowser } from "@/components/property/properties-browser";
export default function SearchPage() {
    return (_jsx(PropertiesBrowser, { title: "Search Rentals", subtitle: "Search by location, budget, property type, and preference with a cleaner, faster flow." }));
}
